mutation addUsersToReadonlyFile(
  $fileID: String!
  $readonlyUserIDs: [String!]!
  $shouldSendMail: Boolean!
  $userMessage: String
) {
  addUsersToReadonlyFile(
    fileID: $fileID
    readonlyUserIDs: $readonlyUserIDs
    shouldSendMail: $shouldSendMail
    userMessage: $userMessage
  )
}
