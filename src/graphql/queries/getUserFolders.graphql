query getFoldersOfFolder($folderID: String!) {
  getFoldersOfFolder(folderID: $folderID) {
    ID
    name
    files {
      ID
      name
      url
      fileType
    }
    subFolders {
      ID
      name
      path
    }
    path
    isTrash
    isPublic
  }
}
